"""Packs — versioned DATA, not code.

Country/sector source packs (search playbooks, apply-form maps,
auto_apply_safe flags) plus their schema validator. Packs are
community-contributable and deploy independently of code releases.
"""
