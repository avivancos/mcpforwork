# S4.4 — Obstacle routes + report_playbook_result capture

**Epic:** Sprint 4 — Browser-apply orchestration
**Estimated effort:** ~2 h
**State:** pending

## Spec

- Obstacles: `report_apply_progress(..., status="blocked",
  obstacle="captcha|login|2fa|hostile_bot_check")` → state stays `filling`,
  response `{pause: true, instruction: the HUMAN resolves it in their browser
  (captchas are always human-solved, never bypassed), resume: call
  report_apply_progress(status=ok) when done}`. Audited with the obstacle.
- Migration v7: `playbook_reports` (id, user_id, source_slug, kind:
  success|drift|break, diff TEXT/JSON, created_at) + RLS. Feeds the S8
  pack-update loop.
- `report_playbook_result(uow, user_id, source_slug, kind, diff)` — opt-in
  capture at session end; unknown source rejected; audited. MCP tool.
- Simplifier pass (carried from S3 gate): refactor the repeated
  `_uow()`/try/finally tool boilerplate in server.py into one
  `_tenant_uow()` contextmanager (20+ call sites).

## Definition of Done

- [ ] Obstacle pause/resume round-trip tested (captcha → human note → ok)
- [ ] playbook_reports persisted + RLS; unknown source/kind rejected
- [ ] _tenant_uow refactor: server.py net-smaller, all tools still green
- [ ] Sprint demo → need_human_testing: exact steps for one REAL application
      via the browser with the human clicking Submit
- [ ] Gate 4/4
