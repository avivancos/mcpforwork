# S7.4 — Packed auto_apply_safe L2 re-probe (follow-up)

**Epic:** Verification / Autopilot L2
**Estimated effort:** ~1 h after a board is curated

## Goal

When a future card ships the first packed `auto_apply_safe: true` board
(with browser evidence on that curation card), re-run the Phase 6 L2
real-employer / real-board probe that S7.3 deferred: policy cap=2 batch,
third refusal, criteria-snapshot audit, revoke interruptibility on that
board.

## Spec

- Depends on a packed source with `apply_playbook.auto_apply_safe: true`
  and evidence on its curation card (never invent the flag here).
- Enable L2 policy min_score/cap=2; run two authorized submits on that
  board; third `request_submit` refused by cap; revoke mid-batch →
  `await_human`.
- Record audit criteria snapshots.

## Files to create/modify

- Evidence on this card; product code only if a defect is found.

## Dependencies added

None.

## Definition of Done

- [ ] Real (or human-verified) batch on a packed safe board
- [ ] Cap + revoke evidence recorded
- [ ] Fused gate if code changed; else N/A

## Improvements noted
<!-- -->

## Pending human testing

Human present for any real-employer submit; same consent rules as S7.3.
