# W2.2 — Docs: Reference + Self-hosting pages

**Epic:** Web / public site (commercial v1)
**Estimated effort:** ~3 h

> Follow-up to W2.1. The docs content system + Getting-started + Core-concepts +
> FAQ shipped in W2.1; the Reference and Self-hosting sidebar items render as
> "coming soon". This card authors them.

## Goal

Author the remaining docs pages so no sidebar item is "coming soon", using the
existing `DocsShell` + `_nav.ts` system (flip `available` to true as each ships):

- **Reference:** MCP tools (the ~14/26-tool surface, grouped by area), Slash
  commands (`/setup` `/hunt` `/review` `/apply` `/interview` `/upskill`),
  Configuration (env vars: `MCPFORWORK_DATA_DIR`, `MCPFORWORK_DB_URL`,
  `MCPFORWORK_TRANSPORT`, etc.), Digest email.
- **Self-hosting:** Install & run (uvx / docker compose — reuse the real W1.2
  compose), Data & backups (SQLite location, export/delete, R2/volume),
  Contribute a pack (schema + PR + live probe).

## Spec

- Same route-per-page pattern; set `available` in `_nav.ts` per page shipped.
- Content accurate to the product (PRODUCT_PLAN §MCP surface, §Prompts, config.py
  env vars, ADR 0003 compose). The MCP-tools count in `_nav.ts` (14) should match
  what the page documents — reconcile with the real server tool list.
- No new dependency.

## Definition of Done

- [ ] All 7 Reference + Self-hosting pages render; `_nav.ts` has no `available:
      false` left; sidebar shows no "coming soon"
- [ ] `npm run build` + `tsc` + guard green; browser-verified
- [ ] Final-review gate PASS

## Improvements noted
<!-- -->
