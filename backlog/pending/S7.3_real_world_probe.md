# S7.3 — Real-world L0/L1/L2 probes + second agent client

**Epic:** Verification / self-host E2E
**Estimated effort:** ~3 h wall-clock (human present for L0/L1 submit)

## Goal

Prove the self-host stack end-to-end at all three consent levels against real
traffic where possible, and prove agent-agnosticism with a second MCP client.
Close the S4 demo card when L0 evidence lands. Honest empty `auto_apply_safe`
(S7.2c) means L2 against a packed safe board is not available — probe the
refusal path + fixture-injectable path instead; do not invent a safe flag.

## Spec

### Probe A — L0 supervised (human clicks Submit)

Reuse `backlog/need_human_testing/S4_demo_real_application.md`:
- Real posting the human wants; agent fills; `request_submit` → `await_human`;
  **human** clicks Submit; `confirm_submitted`.
- Audit: `start → progress → request_submit(await_human) → confirm_submitted`.
- `check_seen` → `applied: true`.
- **FAIL** if the agent clicks Submit or any pre-gate response contains a
  submit instruction.

### Probe B — L1 dashboard approval

- One application filling → dashboard Approve → agent `request_submit` →
  `submit_authorized` with `consent: autopilot_l1` (agent may click Submit
  only after that decision).
- Dashboard chip / application row shows `submitted` + L1 consent stamp.

### Probe C — L2 (honest empty + fixture)

Given S7.2c: no packed `auto_apply_safe` board.
1. **Empty allowlist:** enable L2 policy in dashboard; `autopilot_queue` empty;
   `request_submit` on a normal board → `await_human` / not L2-authorized
   (reason includes not auto_apply_safe). Evidence recorded.
2. **Fixture path (no pack flag):** injectable `safe_sources` + local
   pytest-httpserver page OR existing `tests/test_autopilot_l2.py` evidence —
   cap=2 authorization + third refusal + revoke interruptibility. Not a real
   employer; proves the consent path when a board eventually ships.
3. **Deferred:** first packed `auto_apply_safe: true` re-opens a follow-up
   probe card — never flag a board here without browser evidence.

### Probe D — Second agent client

- Generate config via `mcpforwork connect --client <other> --mode compose`
  (not the client used for Probe A if A used Cursor/Claude).
- Real MCP traffic against compose `:8500` (tool list / `get_profile` or
  equivalent read). Evidence: command + response snippet on this card.

### Close-out

- Move S4 demo card → `done/` when Probe A passes.
- Record pass/fail + evidence per probe below.
- File follow-ups for any gaps.

## Files to create/modify

- `backlog/done/S7.3_real_world_probe.md` (this card, when closed)
- `backlog/need_human_testing/S4_demo_real_application.md` → `done/` on L0 pass
- Evidence notes on this card (no product code expected unless a defect found)
- Optional follow-up card if a defect is found

## Dependencies added

None.

## Definition of Done

- [ ] Probe A (L0) pass/fail recorded (or blocked with written reason + human steps)
- [ ] Probe B (L1) pass/fail recorded (or blocked with written reason + human steps)
- [x] Probe C (L2 empty allowlist + fixture/cap evidence) recorded
- [x] Probe D (second client) real MCP traffic evidence recorded
- [ ] S4 demo card moved to `done/` iff Probe A passed
- [ ] Fused review gate: test-code-reviewer PASS if code changed; else N/A
      with disclosure (verification-only card)
- [x] Follow-ups filed for deferred packed-L2 re-probe (`S7.4`)

## Evidence log

### Probe C — L2 (2026-08-02) — PASS (empty + fixture)

**Live compose (api :8000, mcp :8500):**
1. Magic-link as `local@self-host` → session cookie.
2. `GET /v1/autopilot/boards` → `{"boards": []}` (pre and post policy).
3. `PUT /v1/autopilot/policy` `{min_score:70, max_per_day:2}` → policy
   enabled (`minScore:70`, `maxPerDay:2`).
4. MCP `autopilot_queue` (HTTP) → active policy, `items: []`.
5. Policy revoked after probe (`POST /v1/autopilot/policy/revoke` →
   `policy: null`).

**Fixture path (injectable safe_sources, not a packed flag):**
`uv run pytest tests/test_autopilot_l2.py` — 41 passed earlier; subset
`-k "cap or not_safe or revoke or empty or frozenset or safe_sources"` —
8 passed (incl. not-safe refusal, daily cap hit, revoke mid-batch,
registry-empty queue).

**Deferred:** packed-board L2 → `backlog/pending/S7.4_packed_l2_reprobe.md`.

### Probe D — Second agent client (2026-08-02) — PASS

- Client shape: **Codex compose** URL from
  `mcpforwork connect --client codex --mode compose` →
  `http://localhost:8500/mcp`.
- Traffic: JSON-RPC `initialize` (`clientInfo.name=codex-s73-probe`) →
  `notifications/initialized` → `tools/list` → **40 tools**, including
  `request_submit`, `autopilot_queue`, `confirm_submitted`.
- Session id observed: `9fc8b37862004b8ab923e4ed23510160`.
- This Cursor chat is the other client; Codex-shaped HTTP is the second.

### Probe A — L0 — PENDING human

Blocked on human choosing a real posting and clicking Submit (S4 protocol).

### Probe B — L1 — PENDING human

Blocked on human: dashboard Approve → agent Submit after
`submit_authorized` / `autopilot_l1`.

## Improvements noted

- S7.4 follow-up filed for first packed `auto_apply_safe` re-probe.
- Live PUT `/v1/autopilot/policy` returned an empty body in curl while GET
  showed the policy — worth a tiny follow-up if not already covered by
  roundtrip tests (API may return 204/empty JSON).

## Pending human testing

Probes A and B require the human to choose a real posting and (for A) click
Submit on the employer site. Steps mirror S4 demo card §Pending human testing.

**Suggested human path (compose already up on :2200 / :8000 / :8500):**
1. Open dashboard http://localhost:2200 — magic-link `local@self-host`
   (link prints in `docker compose logs api`).
2. Wire Cursor or Claude via `mcpforwork connect --mode compose`.
3. `/setup` → `/hunt` on reed/infojobs → approve a job you want → `/apply`.
4. L0: stop at `await_human`, **you** click Submit, then
   `confirm_submitted`.
5. L1 (separate app): Approve in dashboard Autopilot/Approvals → agent
   `request_submit` should authorize → agent may click Submit → confirm.


## Pause note

Paused 2026-08-02 for S5.3 (CV-first setup_hints) — human L0/L1 probes still pending; resume after S5.3 closes.
