# S0.2 — Install the development harness

**Epic:** Sprint 0 — Foundations
**Estimated effort:** ~2 h

## Goal

The repo enforces its own rules: four reviewer subagents, the final-review
gate, the backlog kit, and freshly authored `AGENTS.md` + `CLAUDE.md` — so
every later card runs inside the quality system instead of before it.

## Spec

Copy + adapt from `~/Desarrollo/agents-specs` per §Development harness of
`docs/PRODUCT_PLAN.md`:

- `.claude/agents/test-auditor.md` — zero-mocks discipline (real SQLite
  tmp_path, pytest-httpserver, recorded fixtures) replaces the generic mock
  guidance; canonical command `uv run pytest`.
- `.claude/agents/simplicity-reviewer.md` — the anti-over-engineering charter
  embedded as P0-block rules with a charter-compliance output section.
- `.claude/agents/security-reviewer.md` — product P0 surfaces added:
  connector OAuth, magic-link/session auth, RLS/tenant isolation, Stripe
  webhook, consent gate, autopilot policy, zero-LLM invariant.
- `.claude/agents/code-reviewer.md` — as-is; frontmatter `model: inherit`.
- Final-review gate condensed to 4 reviewers (documentation-reviewer
  on-demand) and embedded as `AGENTS.md §6` — no separate workflow file.
- `backlog/` kit: `agent_index.md` + `_TEMPLATE.md` with placeholders filled
  (`<RUN_ENV>`=uv+docker, `<DOC_LANGUAGE>`=English, `<DEFAULT_BRANCH>`=main),
  §3 rewritten to zero-mocks, DoD extended with the gate + dependency
  justification; all seven state dirs.
- `AGENTS.md` authored fresh from the plan's invariants; `CLAUDE.md` as thin
  complement pointing at it; plan checked in at `docs/PRODUCT_PLAN.md`;
  ADR 0001 recording bootstrap decisions.

## Files to create/modify

- `.claude/agents/{test-auditor,simplicity-reviewer,security-reviewer,code-reviewer}.md` — NEW
- `backlog/agent_index.md`, `backlog/_TEMPLATE.md`, state dirs — NEW
- `AGENTS.md`, `CLAUDE.md` — NEW
- `docs/PRODUCT_PLAN.md` — NEW
- `backlog/decisions/0001_bootstrap_decisions.md` — NEW

## Dependencies added

None.

## Definition of Done

- [x] All four reviewer files valid Claude Code subagents (frontmatter parses;
      all four spawned and reported in the wave-1 gate run)
- [x] No `<PLACEHOLDER>` tokens remain anywhere in `backlog/` (grep clean)
- [x] `CLAUDE.md` → `AGENTS.md` links resolve; AGENTS.md covers invariants,
      architecture, charter, testing, backlog, gate, commits, commands
- [x] Targeted tests green via `uv run pytest tests/`
- [x] Final-review gate: all four reviewers PASS (after 4bf8b6e fixes)
- [x] Post-task audit done: the gate itself was exercised end-to-end on this
      sprint's diff — found a real P1 and three convergent P2s, all fixed —
      the Sprint 0 demo gate criterion

## Improvements noted

- `State:` header removed from the card template and skeleton — the folder a
  card sits in is the only state source (gate P2: the field drifted on its
  very first move).
- CLAUDE.md lifecycle text slimmed to a pointer at `agent_index §2` to remove
  a divergent restatement (gate P3).
- AGENTS.md §4 now sanctions manual mutant probes as the substitute for a
  mutmut dependency (gate P3, written rejection of the dep).
