# S0.1 — Bootstrap the repository

**Epic:** Sprint 0 — Foundations
**Estimated effort:** ~1 h

## Goal

A fresh English-first repo at `~/Desarrollo/mcpforwork` that builds, installs,
and lints from a clean checkout — the substrate every later card stands on.

## Spec

- `git init` on branch `main`; canonical Apache-2.0 `LICENSE` text.
- `README.md` stub: product one-liner, the six invariants condensed, dev
  commands, license.
- `pyproject.toml`: uv-managed, hatchling build, package `mcpforwork` under
  `src/` layout, Python `>=3.12`, no runtime dependencies; dev group with
  pytest, pytest-httpserver, ruff, import-linter.
- ruff configured (line length 100, `E F W I UP B SIM`); pytest configured
  (testpaths, `live` marker excluded by default).
- `uv sync` succeeds and `uv run python -c "import mcpforwork"` works.

## Files to create/modify

- `LICENSE` — NEW, canonical Apache-2.0
- `README.md` — NEW
- `pyproject.toml` — NEW
- `uv.lock` — NEW, generated
- `.gitignore`, `.python-version` — NEW
- `src/mcpforwork/__init__.py` — NEW, `__version__`

## Dependencies added

- `pytest` (dev) — the test runner; TDD is a binding rule.
- `pytest-httpserver` (dev) — zero-mocks HTTP testing (AGENTS.md §4).
- `ruff` (dev) — single tool for format + lint.
- `import-linter` (dev) — architecture boundaries enforced in CI, not review.

## Definition of Done

- [x] `uv sync` from clean checkout succeeds; package imports with a version
- [x] `uv run ruff format --check .` and `uv run ruff check .` green
- [x] Targeted tests green via `uv run pytest tests/` (3 passed)
- [x] Final-review gate: all four reviewers PASS (wave 1: 3 PASS + test-auditor
      FAIL on a P1; fixed in 4bf8b6e; wave 2 rerun confirmed — see closing
      commit)
- [x] Post-task audit done (smoke: `import mcpforwork` printed 0.1.0; dist
      version resolves via importlib.metadata)

## Improvements noted

- Version is now single-sourced from `__init__.py` via hatch dynamic
  versioning (gate P3: pyproject duplicated it).
- Dependency justifications recorded on this card per the charter.
