# S6.5 — Truthful MCP guidance + /apply prompt

**Epic:** Sprint 6 / self-host E2E
**Estimated effort:** ~1 h

## Goal

Replace stale "arrives in later sprints" guidance with the real shipped loop
(brief → draft → coverage → start_application → progress → request_submit →
human submits → confirm_submitted), and fix the outdated config docstring.

## Spec

1. `SERVER_INSTRUCTIONS` step 3 describes review + assets + supervised apply
   (no "later sprints"). Mentions `/review`, `/apply`, `start_application`,
   `request_submit`, human clicks Submit, `confirm_submitted`.
2. `/apply` prompt covers the full orchestration loop end-to-end (not just
   draft materials). Still asserts the human clicks Submit at L0.
3. `config.local_user_id` docstring no longer says magic-link "arrives in a
   later sprint" (it shipped in S6.1).
4. Tests in `tests/test_mcp_server.py` assert the new wording (and reject the
   stale phrases).

## Files to create/modify

- `src/mcpforwork/entrypoints/mcp/guidance.py`
- `src/mcpforwork/entrypoints/mcp/server.py` (`apply_session` prompt)
- `src/mcpforwork/config.py`
- `tests/test_mcp_server.py`

## Dependencies added

None

## Definition of Done

- [x] No "later sprint(s)/version" stale claims in guidance or /apply prompt
      (grep across src/ + docs/ empty — verified by review gate)
- [x] Tests assert the real apply loop verbs
      (`test_server_instructions_describe_the_shipped_apply_loop`,
      `test_apply_prompt_covers_the_full_orchestration_loop`; consent sentence
      pinned via "clicks submit"/"never click submit")
- [x] Targeted tests green via `uv run pytest tests/test_mcp_server.py` (9 passed)
- [x] Fused review gate: test-code-reviewer PASS incl. regression audit —
      full suite 315 passed, lint-imports 8/0, ruff clean, consent grep empty,
      no gate/test weakening; P2 + 2×P3 findings fixed and re-verified PASS
- [x] Post-task audit: `server_info` instructions payload read truthful at
      runtime by the review gate (all named tools exist in the 37-tool registry)

## Improvements noted

- None. (Breadcrumb/prompt wording tidied in-card per review findings.)
