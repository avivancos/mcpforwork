# S6.10 — API structured error kinds + action test gaps

**Epic:** Sprint 6 / self-host E2E — hardening follow-up (from S6.6b review)
**Estimated effort:** ~1.5 h

## Goal

Replace the `"not found" in error` string-sniffing in
`entrypoints/api/app.py::_action_response` with structured error kinds, and
close the test gaps the S6.6b review flagged.

## Spec

- Services (`review`, `apply`) return an error kind alongside the message
  (e.g. `{"error": ..., "kind": "not_found" | "invalid_state" | ...}`); the
  API maps kinds → status codes (404/400). No substring matching.
- Add the missing tests:
  - outcome route with the application in `filling` → 400 (invalid
    transition via API, not just via approve).
  - foreign-match 404 for discard / restore / outcome (approve already has
    one).
  - `restore_match` audit row asserted.
  - profile POST anti-smuggling: a body with raw intake keys (`full_name`,
    `label`, `min_salary_amount`) does NOT write those columns.

## Files to create/modify

- `src/mcpforwork/services/review.py`, `services/apply.py` (error kinds)
- `src/mcpforwork/entrypoints/api/app.py` (kind → status mapping)
- `tests/test_match_actions_api.py` (+ the MCP tools' error paths stay
  message-only — the kind is additive)

## Dependencies added

None

## Definition of Done

- [x] No substring error matching left in the API entrypoint —
      `_action_response` maps `_KIND_STATUS` (not_found→404, invalid_state→400,
      invalid_input→400, default 400); grep over `src/` finds zero
      `"not found" in` occurrences
- [x] The four test gaps covered: outcome on `filling` → 400 via API;
      foreign-match 404 for discard/restore/outcome; restore_match audit row
      asserted; profile POST anti-smuggling (raw intake keys do not write)
- [x] Targeted tests green (`tests/test_match_actions_api.py` 20 passed);
      full suite 370 passed; live-PG 35 passed
- [x] Fused review gate: test-code-reviewer PASS (incl. regression audit)

## Improvements noted

- Error kinds are additive: the MCP tools keep reading only `error`
  (message-only contract unbroken); `kind` is never read by the MCP
  entrypoint.
- `review.py` gained a `_not_found` helper (3 use sites — rule of two) so
  unknown and foreign matches stay indistinguishable by construction.
- Gate P2 disposition (fixed on this card): the "revert to substring check"
  mutant survived because it is behaviorally identical today — killed
  structurally with a new CI grep-guard in `.github/workflows/ci.yml`
  (`"not found" in` under `entrypoints/api/` fails the build), verified
  locally empty.
- Gate P3s noted, no action: `record_outcome` docstring doesn't restate the
  kind contract (the review.py module docstring does); the concurrent-change
  branch (`rowcount == 0`) remains covered only transitively — simulating a
  real race is out of scope here.
- Evidence at close: targeted 20 passed · full 370 passed · live 35 passed
  (Docker postgres:16-alpine) · ruff format/check clean · lint-imports 8 kept
  0 broken · consent grep empty · zero mocks.
