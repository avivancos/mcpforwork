# S0.3 — Hexagonal skeleton + boundary contracts + CI

**Epic:** Sprint 0 — Foundations
**Estimated effort:** ~1.5 h

## Goal

The §Architecture spec layout exists as code and its boundary rules fail the
build when violated — architecture drift becomes impossible to merge, not a
review comment.

## Spec

- Packages `src/mcpforwork/{domain,services,ports,adapters,entrypoints,packs}`,
  each `__init__.py` docstring stating the layer's boundary rule. No
  speculative subpackages (rule of two — `adapters/db/` arrives with S1.1).
- import-linter contracts in `pyproject.toml`:
  1. domain imports no other layer;
  2. services never import adapters/entrypoints;
  3. ports are pure seams (no services/adapters/entrypoints);
  4. no `openai`/`anthropic` importable from domain/services/ports/packs.
  (Entrypoint-independence contract deferred until a second entrypoint
  exists — noted for S2.1.)
- `.github/workflows/ci.yml`: uv sync --locked → ruff format --check → ruff
  check → lint-imports → consent-verb grep gate (`submit_authorized` only
  under `src/mcpforwork/services/`) → pytest.

## Files to create/modify

- `src/mcpforwork/{domain,services,ports,adapters,entrypoints,packs}/__init__.py` — NEW
- `pyproject.toml` — import-linter contracts
- `.github/workflows/ci.yml` — NEW

## Dependencies added

None.

## Definition of Done

- [x] `uv run lint-imports` reports all contracts KEPT (4/4)
- [x] A deliberate violation breaks lint-imports locally (two probes, both
      reverted: domain→adapters import → exit 1; `import openai` in
      services → contract BROKEN, exit 1)
- [x] Targeted tests green via `uv run pytest tests/`
- [x] Final-review gate: all four reviewers PASS (after 4bf8b6e fixes)
- [x] Post-task audit done (smoke: every CI step run locally with recorded
      output; consent gate probed with a sneaky in-comment path line —
      detected)

## Improvements noted

- Consent-verb gate hardened twice: first from line-content filtering (fail-
  open via comment trick — gate P2) to `--exclude-dir`, then — after the
  rerun showed `--exclude-dir` matches nested `*/services/` dirs (P3 from
  three reviewers) — to a path-anchored exclusion
  (`grep -v '^src/mcpforwork/services/'`). Probed against both bypasses and
  the sanctioned placement.
- `actions/checkout` SHA-pinned (v4.2.1) alongside setup-uv per the
  security-reviewer residual.
- Zero-LLM contract widened from 4 layers × 2 SDKs to the whole package × 9
  SDK roots (gate P2 from test-auditor + security-reviewer).
- CI hardened: `permissions: contents: read`, setup-uv pinned to the v5.4
  commit SHA (gate P2 from security-reviewer).
- Entrypoint-independence contract deferred to S2.1 — carried in ADR 0001 so
  it survives this card closing (gate follow-up).
