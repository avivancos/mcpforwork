# S6.0 — abandon_application tool (close stuck open sessions)

**Epic:** Apply orchestration follow-up (S4 gate)
**Estimated effort:** ~1 h
**State:** pending

## Goal
An application the human declines to submit is currently stuck open forever
(start_application perpetually re-opens it; reports rejected outside filling).
Add `abandon_application(application_id, reason?)`: any open state → abandoned
(via can_transition), audited; start_application then creates a fresh session.

## Definition of Done
- [x] abandon from filling/awaiting_human/submit_requested; rejected from
      submitted/verified; audited; foreign id rejected
- [x] after abandon, start_application creates a NEW application
- [x] Tool-layer test with fresh-connection check; gate 4/4

## Closure (2026-07-11)
`services/apply.abandon_application` (load → `can_transition` guard → user-scoped
UPDATE → audit) + `@mcp.tool()` wrapper + guidance breadcrumb. 10 tests
(`tests/test_abandon.py`), 257 sqlite + 22 live PG green. Gate 4/4 PASS
(code/test/simplicity on Sonnet 5 high, security on Opus): zero P0/P1; test-auditor
mutant probes killed the can_transition-removal, cross-tenant-scope-removal, and
dropped-commit mutants; P2/P3 test-hardenings applied (verified-state rejection,
awaiting_human read-back, default-reason). Reused the existing `abandoned`
terminal transition and `_OPEN_STATES` exclusion — no new abstraction/dep.
Carried (non-blocking): shared free-text length cap across audit params when the
hosted multi-tenant path lands (→ ADR 0001).
