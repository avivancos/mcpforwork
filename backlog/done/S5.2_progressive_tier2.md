# S5.2 — Progressive Tier 2: profile_gaps + guided import prompts

**Epic:** Sprint 5 — Self-host onboarding + packaging
**Estimated effort:** ~2 h
**State:** pending

## Spec
- `services/profiles.profile_gaps(uow, user_id)`: which Tier-1/Tier-2 fields
  are missing + one-line why-it-matters (achievements bank = highest leverage;
  style sample; links; deal_breakers; availability). Ordered: required gaps
  first, then progressive.
- MCP tool `profile_gaps`; /setup prompt gains the progressive step ("after
  Tier 1, offer ONE gap at a time when contextually useful, never a form-wall").
  LinkedIn guided import stays via import_from_url_findings (S2.2 tool).

## Definition of Done
- [ ] Empty profile -> required gaps listed first; full profile -> empty list
- [ ] Tool + prompt wired; targeted tests green; gate next wave
