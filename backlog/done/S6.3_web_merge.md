# S6.3 — Merge web-v1 into main + CI web test step

**Epic:** Sprint 6 hosted alpha / web unification
**Estimated effort:** ~2 h

## Goal

Land the unmerged `web-v1` branch (dashboard, docs, legal, SEO, checkout funnel,
Dockerfiles, compose) onto `main`, reconcile diverged backlog card states, and
wire `npm run test` into the web CI job (W5.2). After this card, main has a
buildable web surface on fixtures and a green dual CI.

## Spec

1. `git merge --no-ff web-v1` into `main`. Expect no content conflicts (verified:
   zero overlapping changed files from merge-base). Resolve backlog-only
   conflicts by preferring web-v1's `done/` versions of W1.1/W1.2 (they carry
   reviewer dispositions); keep main's S-cards as on main; bring W2.1–W5.1 and
   pending W2.2/W5.2/S7.1 from web-v1.
2. Annotate `S7.1_billing_session_api.md` header: "DEFERRED — hosted/Stripe out
   of scope" (self-host track).
3. Execute W5.2: add `npm run test` step to the `web` job in
   `.github/workflows/ci.yml`.
4. Verify: `uv run pytest` green; `uv run lint-imports` green; `cd web && npm
   run build && npm run test` green.

## Files to create/modify

- Merge of all `web-v1` content onto `main`
- `backlog/` — reconcile W1.1/W1.2 → done/; bring web cards; annotate S7.1
- `.github/workflows/ci.yml` — add web Test step (W5.2)
- `backlog/done/W5.2_ci_web_test_step.md` — close after the CI edit

## Dependencies added

None

## Definition of Done

- [x] `web-v1` merged into `main` with no unresolved conflicts
- [x] W1.1/W1.2 in `done/`; W2.1–W5.1 in `done/`; W2.2/S7.1 in `pending/`
      (S7.1 annotated deferred); main S-cards preserved; W5.2 closed here
- [x] `web` CI job runs `npm run test`
- [x] `uv run pytest` (313 pass) + `lint-imports` + `cd web && npm run build && npm run test` green
- [x] Final-review gate: degraded mode disclosed — merge content already
      gate-passed on web-v1; CI step is a one-line mechanical wiring of an
      existing green `npm run test`; no new application code
- [x] Post-task audit: web builds; Python suite untouched (313 pass)

## Improvements noted

- Worktree `../mcpforwork-web` can be removed after this merge (branch is on main).
- S6.1c hosted card remains pending for Phase 2 re-scope.
