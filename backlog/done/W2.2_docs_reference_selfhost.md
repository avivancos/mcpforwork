# W2.2 — Docs: Reference + Self-hosting pages

**Epic:** Web / public site (commercial v1)
**Estimated effort:** ~3 h

> Follow-up to W2.1. The docs content system + Getting-started + Core-concepts +
> FAQ shipped in W2.1; the Reference and Self-hosting sidebar items render as
> "coming soon". This card authors them.

## Goal

Author the remaining docs pages so no sidebar item is "coming soon", using the
existing `DocsShell` + `_nav.ts` system (flip `available` to true as each ships):

- **Reference:** MCP tools (the ~14/26-tool surface, grouped by area), Slash
  commands (`/setup` `/hunt` `/review` `/apply` `/interview` `/upskill`),
  Configuration (env vars: `MCPFORWORK_DATA_DIR`, `MCPFORWORK_DB_URL`,
  `MCPFORWORK_TRANSPORT`, etc.), Digest email.
- **Self-hosting:** Install & run (uvx / docker compose — reuse the real W1.2
  compose), Data & backups (SQLite location, export/delete, R2/volume),
  Contribute a pack (schema + PR + live probe).

## Spec

- Same route-per-page pattern; set `available` in `_nav.ts` per page shipped.
- Content accurate to the product (PRODUCT_PLAN §MCP surface, §Prompts, config.py
  env vars, ADR 0003 compose). The MCP-tools count in `_nav.ts` (14) should match
  what the page documents — reconcile with the real server tool list.
- No new dependency.

## Definition of Done

- [x] All 7 Reference + Self-hosting pages render; `_nav.ts` has no `available:
      false` left; sidebar shows no "coming soon"
- [x] `npm run build` + `tsc` + guard green; browser-verified
- [x] Final-review gate PASS

## Evidence at close

- 7 pages authored on the DocsShell pattern: mcp-tools, slash-commands,
  configuration, digest-email, install, data-backups, contribute-a-pack.
- `_nav.ts`: all `available: false` removed; MCP-tools count reconciled
  14 → **38** (the real `@mcp.tool()` count in server.py, grouped 10 profile /
  6 hunt / 5 review / 5 generation / 9 apply / 3 privacy-meta —
  `import_from_url_findings` documented under Profile, its real semantics).
- Spec deviation (truthfulness): the card's `/interview` and `/upskill`
  prompts do not exist — the server ships exactly 4 prompts (setup, hunt,
  review, apply); the slash-commands page documents the real four.
- Digest-email page is honest about self-host: no mailer/scheduler self-host
  (console mailer prints magic links); digest is hosted-track scope.
- `npm run test` 10/10 · `tsc --noEmit` clean · `npm run build` green with
  all 7 routes static · no-LLM-deps guard OK.
- Browser-verified against `next dev`: all 7 routes 200; docs index has zero
  "coming soon"; mcp-tools sidebar badge shows 38; install page renders
  uvx + compose + connect sections.

## Review dispositions (fused gate, PASS on 2nd run)

- First run FAIL — 6 P1 accuracy defects (never-fabricate applies to docs):
  wrong `MCPFORWORK_USER_EMAIL` default, wrong API host/port defaults,
  invented `MCPFORWORK_API_URL` default, wrong HTTP methods for
  export/delete, wrong web port (3000 vs 2200), misdescribed
  `import_from_url_findings`. All fixed and re-verified against code.
- [P2 fixed] `PUBLIC_BASE_URL` default → "unset (request origin)";
  `parse_cv` → read-only extraction wording.
- [P3 fixed] `get_asset_file` wording; data-backups mentions derived
  `assets/` dir; `USER_ID` "legacy" → "optional".
- [P3 rejected] `ALLOWED_HOSTS` omits the test-only `testserver` entry —
  correct for user-facing docs.
- Follow-up candidate (not carded): a drift-guard test asserting the
  `_nav.ts` mcp-tools count equals the server's `@mcp.tool()` count.

## Improvements noted
<!-- -->
