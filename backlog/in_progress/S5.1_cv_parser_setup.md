# S5.1 — Zero-LLM CV parser + /setup wiring

**Epic:** Sprint 5 — Self-host onboarding + packaging
**Estimated effort:** ~3 h
**State:** pending

## Spec
- `domain/cv.py` (PURE, port of donor services/onboarding.py parser path):
  `extract_profile_from_cv(cv_text) -> {candidate: {full_name, contact_email,
  phone, linkedin, github, portfolio}, cv_text}`. Compiled regexes
  (email/phone/URL/LinkedIn/GitHub), header-block bounding (section headers
  EXPERIENCE/EDUCATION/... never mis-extracted as a name), low-confidence ->
  None (never invent). NO LLM path at all (ours is structurally zero-LLM).
- MCP tool `parse_cv(cv_text)` -> extracted fields + instruction to CONFIRM
  with the human before update_profile. /setup prompt references it.

## Definition of Done
- [ ] Donor fixture behaviors: name from label or first header line; None when
      header starts with email/URL/section; email anywhere; phone only from
      header (>=7 digits, dates rejected); linkedin/github/portfolio split
- [ ] parse_cv tool returns fields + never writes the profile itself
- [ ] Targeted tests green; gate next wave
