# S5.3 — CLI init/serve + uvx packaging + README quickstart

**Epic:** Sprint 5 — Self-host onboarding + packaging
**Estimated effort:** ~2 h
**State:** pending

## Spec
- `entrypoints/cli/main.py` (stdlib argparse, no Typer — charter):
  `mcpforwork init` (create data_dir, migrate the SQLite db, print .mcp.json
  snippet) · `mcpforwork serve` (exec the stdio MCP server) · `mcpforwork
  version`. Console script `mcpforwork`.
- Entrypoint-independence import contract NOW (2nd entrypoint exists — carried
  ADR item): cli and mcp never import each other... except serve delegating to
  the mcp server main — allowed via explicit contract exception? NO: serve
  re-execs `mcpforwork.entrypoints.mcp.server:main` via import — that IS an
  entrypoint import. Decision: the independence contract forbids api<->cli
  <->mcp EXCEPT cli->mcp.server.main (serve is a launcher). Simplest honest
  contract: forbid mcp/api importing cli and api<->mcp; document cli->mcp as
  the sanctioned launcher edge.
- README quickstart: uvx mcpforwork init/serve + .mcp.json snippet.
- Demo gate: clean tmp HOME -> init -> serve smoke -> onboard -> hunt plan,
  zero LLM calls.

## Definition of Done
- [ ] init creates db (version = SCHEMA_VERSION) + prints connector snippet
- [ ] serve execs the stdio server (smoke: --help + version paths tested;
      in-process server_info covers the serving surface)
- [ ] import contract added; README quickstart section
- [ ] Targeted tests green; gate next wave
